# Uvicorn
uvicorn app.main:app
http://127.0.0.1:8000
uvicorn app.main:app --reload


# Migration
alembic init -t async migration

alembic revision --autogenerate -m "is_user=false"

# Docker
docker-compose up -d